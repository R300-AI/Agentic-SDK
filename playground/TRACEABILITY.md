# Playground V2 Blueprint Traceability

This file records implementation alignment for the current `playground` frontend package. It is intentionally strict: items that require Gateway, SDK, real AI Hub credentials, or full workflow execution are marked as not satisfied instead of being treated as UI-complete.

## Document Coverage

The repository currently contains 22 blueprint files: `00`, `01`, `02`, `04`, `05`, `06`, `07`, `08`, `09`, `10`, `11`, `13`, `15`, `16`, `17`, `20`, `21`, `24`, `25`, `26`, `29`, and `30`. Files `03`, `12`, `14`, `18`, `19`, `22`, `23`, `27`, and `28` are not present in the workspace and cannot be implemented against directly.

## Current Alignment Matrix

| Blueprint | Current status | Evidence in `playground` | Remaining gap |
| --- | --- | --- | --- |
| `00_master_index.md` | Partial | Flask package exists and follows V2 direction | Missing docs remain outside implementation scope |
| `01_product_charter.md` | Partial | Code-first package, no React Flow, Python source kept as canonical preview/export input; Builder has source-backed README starter patterns for minimal, OpenAI client, and custom Action workflows; Runner execute uses supported-source parser metadata with SDK workflow; Runner save calls the AI Hub save API when a session has login credentials and `agent_id` | Real AI Hub config load and arbitrary `python_source` execution are not complete |
| `02_roles_modes_permissions.md` | Partial | `ModeContext` controls edit/save/export/code visibility; untrusted AI Hub editable query/config load is downgraded to readonly; trusted editable mode shows update/save-back context; anonymous save is rejected; AI Hub config endpoints reject disallowed origins; login credentials are kept behind a server-side session ticket for save-back | Broader production credential storage/revocation remains pending |
| `04_user_journeys.md` | Partial | Entry -> Builder -> Runner path exists; AI Hub readonly/editable query path exists; editable Runner states that the workflow was loaded from AI Hub and can be saved back; Runner UI calls source-aware SDK execute plus export/save endpoints; anonymous cannot save via endpoint and sees explicit local/export-only save status | True AI Hub flow and execution of arbitrary Python source still incomplete |
| `05_route_state_map.md` | Partial | `/playground/` skips Entry for AI Hub deep links; untrusted editable links/config loads downgrade to readonly; execute/profile/source/export and AI Hub config routes exist; successful AI Hub save is recorded in session | Execute uses supported generated subset metadata, not arbitrary Python execution; config load remains stubbed |
| `06_page_entry.md` | Partial | Entry has an AMD-inspired centered authentication card, Taiwan Chinese login/local-trial paths, inline error, AI Hub deep links skip the page, and login calls the AI Hub auth API | Full token-based AI Hub session exchange is not available in the current API contract |
| `07_page_builder.md` | Partial | AMD-inspired horizontal setup rail, centered current-step card, switchable single-step panels, Back/Continue rhythm, Taiwan Chinese task language, summary panel, source-derived Run Readiness checklist, supported template/input choices including Document/Form update source and round-trip summary | Full semantic validation and all-step parameter sync are not complete |
| `08_page_runner.md` | Partial | Direct-use AMD-inspired agent page, conversation surface, source-derived chat/result/form surfaces, primary input/result, localized attachment trigger, readable attachment input error, trust toggle, session-backed recent runs, anonymous local/export-only status, editable AI Hub update context, mode-aware creator controls, source-aware SDK execute, export/save actions, and AI Hub save status | Arbitrary Python execution, full runtime attachment handling, and save without an AI Hub `agent_id` remain unsupported |
| `09_visual_north_star_and_references.md` | Partial | Shared AMD-inspired black/white/red token set with low-radius controls, angular cut corners, hard-edged cards, static visual contract checks for AMD tokens/clip paths, and r7 Playwright screenshots in `visual-audit/` showing Entry, Builder, and Runner in Taiwan Chinese | Needs formal PM/design sign-off and brand review |
| `10_component_system.md` | Partial | Button, input, card, context bar, step rail, source-backed starter template picker, Run Readiness checklist, input composer, localized attachment trigger, artifact list, result card, scene-profile-aware context panel, live adoption-state badge, trust toggle, actionable next-step strip, history panel, code drawer, toast/save panel exist | Richer artifact/result variants remain incomplete |
| `11_visual_design_tokens_motion.md` | Partial | Shared AMD-inspired tokens, low radius, angular clip paths, surfaces, responsive rules, productized localized attachment control, toast entrance motion, code drawer transition, centered Entry auth card, centered Builder wizard, direct-use Runner surface, and screenshot-reviewed heading density exist | Broader motion audit not complete |
| `13_python_source_of_truth_spec.md` | Partial | `python_source` stored in session; Builder supported README starter patterns, Document/Form input, and advanced choices rebuild README-style public API source; summaries and runner profile derive from source; limited AST parser round-trips generated subset; unsupported-source execution safety test confirms arbitrary code is not run; no YAML/JSON canonical store | Arbitrary Python round-trip and all Builder fields are not source-backed yet |
| `15_aihub_integration_contract.md` | Partial | AI Hub route/client boundary exists, verifies login credentials through the AI Hub auth API, keeps credentials server-side behind a session ticket, downgrades untrusted editable loads, renders trusted editable update context, rejects anonymous save, checks allowed origins, calls the real AI Hub save API, and records latest successful save status | Real AI Hub config load API and production-grade credential ticket storage are not implemented |
| `16_flask_app_architecture.md` | Partial | Routes/services/models/templates/static are separated; `aihub_client`, source routes, Builder source-update route, source-aware Runner execution service, and a Runner trust partial exist | Full template partialization and arbitrary Python execution remain missing |
| `17_frontend_js_architecture.md` | Partial | Suggested Entry/Builder/Runner/shared JS module files now exist; Builder and Runner page scripts delegate behavior to submodules including history/artifact/save/dialog/adoption-state/next-step helpers; trust disclosure has a template partial; Builder supported selections including Document input persist through source updates | Some modules remain thin helpers and not all Builder parameters are wired |
| `20_accessibility_responsive_spec.md` | Partial | Labels, role/status live regions, trust `aria-expanded`/`aria-controls`, attachment list label, localized attachment trigger, readable input error status, login loading lock, responsive stacking, static keyboard/accessibility tests, responsive/mobile-safe contract tests, r7 mobile screenshots, and Playwright keyboard smoke audit for trust disclosure exist | Manual screen-reader validation and full keyboard audit not done |
| `21_testing_acceptance_matrix.md` | Partial | Smoke tests cover route rendering, assets, template partial rendering, Taiwan Chinese UI copy, static AMD visual/a11y/responsive contracts, mode permissions, AI Hub downgrade/editable context, origin rejection, anonymous save rejection/status copy, AI Hub auth/save client behavior, README starter source generation, Builder readiness checklist, source preview/export, source parser, unsupported-source safety, profile, scene-profile context labels, source-aware SDK execute, readable attachment errors, attachment/save/history/next-step hooks, source-backed advanced Builder choice, key accessibility hooks, first-minute Runner signals, local r7 Playwright screenshot audit, and trust-toggle keyboard smoke audit | No human usability test, arbitrary Python execution, or real AI Hub load tests yet |
| `24_builder_persona_modes.md` | Partial | Default depth uses task language across all steps; README starter choices and Document/Form input choices are source-backed; Run Readiness checklist stays source-derived; advanced mapping is collapsed, lists technical families, and exposes a source-backed structured Action choice | Advanced depth still does not expose all module parameters |
| `25_runner_layout_variants.md` | Partial | `RunnerSceneProfile` is derived from Python source and exposes `chat_first`/`form_first`/`result_first` fit levels; default Runner now renders as a chat-first conversation surface, while Builder Document/Form input and advanced choices drive minimal result-first/form-first surfaces and matching context labels | Full slot composition and richer dedicated variant layouts are not implemented |
| `26_trust_adoption_layer.md` | Partial | Result has conclusion, reason, basis toggle partial, actionable next steps with status feedback, live adoption label; execute updates adoption state and basis from SDK workflow id, visited modules, and parsed source subset metadata; trust basis toggle passes a Playwright keyboard smoke audit | Evidence is not yet Gateway-backed with source snippets or structured citations |
| `29_visual_art_consistency_spec.md` | Partial | Shared AMD-inspired CSS token family across Entry/Builder/Runner; Runner attachment/toast/history components use the same hard-edged cards, borders, low radius, red accents, and Taiwan Chinese CTA language; static visual/responsive tests protect shared family; r7 desktop/mobile screenshots verify the corrected Entry, centered Builder wizard, and direct-use Runner | Needs formal design/PM sign-off and pixel-diff baseline |
| `30_implementation_gap_matrix.md` | Partial | Frontend package begins P0 Builder/Runner replacement, supports README starter source patterns, and route execution now reaches SDK workflow with supported-source metadata and source-backed trust evidence | Gateway result/evidence contract, arbitrary Python execution, and SDK P0/P1 gaps remain |

## Current Rule

Do not mark a blueprint requirement as satisfied unless it is implemented and covered by focused validation. UI placeholders count as partial only.