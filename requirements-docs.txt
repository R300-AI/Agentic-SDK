# Documentation site build dependencies.
# Docs Pages uses this pinned toolchain for strict MkDocs builds.
# Re-run trigger: previous run was cancelled by a GitHub Actions scheduling issue, not a dependency change.
mkdocs==1.6.1
mkdocs-material==9.7.6
pymdown-extensions==10.15
